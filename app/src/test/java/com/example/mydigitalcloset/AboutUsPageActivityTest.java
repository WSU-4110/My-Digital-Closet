package com.example.mydigitalcloset;

import static org.junit.Assert.*;

import android.content.Intent;

import org.junit.After;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

public class AboutUsPageActivityTest {

    //private AboutUsPageActivity activity;


    @Before
    public void setUp() throws Exception {
        //activity = new AboutUsPageActivity();
    }

    @After
    public void tearDown() throws Exception {
        //activity = null;
    }

    @Test
    public void onCreate() {
        //activity.onCreate(null);
        //assertNotNull(activity);

    }

    @Test
    public void openContactForm() {
        //activity.openContactForm();
        // Check that the correct activity was started
        //Intent expectedIntent = new Intent(activity, ContactForm.class);
        //assertEquals(expectedIntent.getComponent(), activity.getStartedActivityIntent().getComponent());

    }
}